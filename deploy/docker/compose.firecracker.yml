services:
  sysbox-api:
    volumes:
      # Firecracker substrate: KVM device plus explicit host-provided tools.
      - /dev/kvm:/dev/kvm
      - ${SYSBOX_TOOLS_DIR:-~/.local/bin}:/opt/sysbox/bin:ro
