services:
  sysbox-web:
    image: sysbox-web:latest
    container_name: sysbox-web
    restart: unless-stopped
    depends_on:
      sysbox-api:
        condition: service_started
    ports:
      - "${SYSBOX_WEB_HOST_ADDR:-0.0.0.0}:${SYSBOX_WEB_HOST_PORT:-3001}:3000"
